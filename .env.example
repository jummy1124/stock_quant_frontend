# 反向代理(同源)架構下，預設不需設定此值 —— 留空即可，前端會用相對路徑 /api，
# 由 nginx (正式) 或 vite proxy (開發) 代理到後端。
#
# 只有在「要讓瀏覽器直連另一台後端」時才填完整 URL（此時後端需自行開 CORS）：
# VITE_API_BASE_URL=http://localhost:8000

# 開發時若篩選後端不在 localhost:8000，可改 vite proxy 目標：
# BACKEND_URL=http://localhost:8000

# ---- 使用者資料服務 (stock_quant_userdata：帳號 + 個股紀錄) ----
# dev 由 vite proxy 把 /userapi 代理到此服務，預設 http://localhost:8100。
# 若 userdata 後端不在此位址可覆寫：
# USERDATA_URL=http://localhost:8100

# 是否啟用 MSW 模擬（假 auth + records，存 localStorage）。
# 預設不設 = 不啟用 → dev 直連真後端 (localhost:8100)。
# 想在沒有後端時開發前端，才設成 true：
# VITE_ENABLE_MSW=true
